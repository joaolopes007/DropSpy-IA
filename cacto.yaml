# Cacto Hosting Configuration
version: 1
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    health_check:
      path: /api/health
      interval: 30s
      timeout: 10s
      retries: 3
